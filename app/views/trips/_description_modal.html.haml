#descriptionModal.modal.hide.fade{"aria-hidden" => "true", "aria-labelledby" => "myModalLabel", :role => "dialog", :tabindex => "-1"}
	.modal-header
		%button.close{"aria-hidden" => "true", "data-dismiss" => "modal", :type => "button"} ×
		%h3#descriptionModalLabel Give Your Trip a Summary
	.modal-body
		= form_for @trip, :html => { :multipart => true }, :remote => true do |f|
			- if @trip.errors.any?
				#error_explanation
					%h2= "#{pluralize(@trip.errors.count, "error")} prohibited this airport from being saved:"
					%ul
						- @trip.errors.full_messages.each do |msg|
							%li= msg
			.field
				= f.label :description
				= f.text_field :description
			%button.btn{"aria-hidden" => "true", "data-dismiss" => "modal"} Cancel
			%button
				= f.submit 'Save'